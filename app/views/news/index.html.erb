<h2>Most recent news</h2>
<% @news.each do |news| %>
<h5><%= link_to news.title, news_path(news)%></h5>
<%= news.content %><br><br/>
<% end %>
<%= link_to 'Create news article', new_news_path, class: 'button inc' %>
